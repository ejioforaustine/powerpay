/*import 'package:flutter/material.dart';

import 'Api testings.dart';

class viewAdmin extends StatefulWidget {
  const viewAdmin({super.key});

  @override
  State<viewAdmin> createState() => _viewAdminState();
}

class _viewAdminState extends State<viewAdmin> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView.builder(
          itemCount: Allbalance.length,
          itemBuilder: (context, index){
            return ListTile(
              title: Text('${Allbalance[index]['balance']}'),
              trailing: Text('${Allbalance[index]['account_reference']}'),
            );

          } ),
    );
  }
}*/

